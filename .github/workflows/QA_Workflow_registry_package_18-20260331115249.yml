name: QA_Workflow_registry_package_18
on:
  registry_package:
    types: [published]
jobs:
  qa-job:
    runs-on: ubuntu-latest
    steps:
      - name: QA Step
        run: echo QA-Workflow original step
