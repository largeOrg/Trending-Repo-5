name: QA_Workflow_workflow_run_27
on:
  workflow_run:
    workflows: ['CI']
    types: [completed]
jobs:
  qa-job:
    runs-on: ubuntu-latest
    steps:
      - name: QA Step
        run: echo QA-Workflow original step
