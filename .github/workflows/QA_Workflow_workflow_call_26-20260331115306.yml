name: QA_Workflow_workflow_call_26
on:
  workflow_call:
    inputs:
      config-path:
        required: false
        type: string
jobs:
  qa-job:
    runs-on: ubuntu-latest
    steps:
      - name: QA Step
        run: echo QA-Workflow original step
